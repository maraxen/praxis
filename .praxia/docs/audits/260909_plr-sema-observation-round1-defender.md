---
title: 'plr-sema increment 7 (the observation record, spec_version 1) -- adversarial round 1, defender'
description: 'Defender pass (praxia:spec-defender, Opus) adjudicating challenger C1-C26: 13 conceded (C3 C4 C5 C6 C9 C11 C13 C14 C15 C20 C22 C23 C24), 13 partial, 0 rebutted outright. C1 arithmetic confirmed at the pin but D5b repriced as a site-keyed semantic model of a named PLR function body -- a NEW kind of hand-maintained fact needing a new row against a full BUDGET_CAP, not HM-25 units; C3 confirmed against the shipped contract table (:321 predicate carries no EnvRef, `resource` is a for-target); C5 confirmed on traceback order (extract_tb[-1] is the backend frame); C19 rebutted on the mechanism (scope_trail records for headers, unsatisfiable under ways 1-3) with a one-sentence remedy. Six defender gaps: D-G1 Q-BIND is dead machinery (:409 goes through _eval_alpha_existential, already element-wise); D-G2 the gate needs D5b AND a repaired D2 together; D-G3 the deck observation may bypass the cache key; D-G4 unknown_ledger does not carry scope_verdict; D-G5 t30_measure prediction role unmaintained; D-G6 :375/:383 carry reachability_clear=false so the D5b discharge is one-directional by construction. NEW user decision D6: accept site-keyed semantic models of named PLR function bodies as a hand-maintained class (a cap conversation). 14-step ordered remediation list. Verdict needs_revision.'
status: final
task_id: 260909_sema-observation
date: '260909'
sprint: '130'
backlog_ids: '5022'
---
> Persisted verbatim by the orchestrator from the defender agent's final report (no write tool).
> Target: `.praxia/docs/specs/260909_plr-sema-observation-increment.md` at commit `7893168a`
> (spec_version 1); challenger report `.praxia/docs/audits/260909_plr-sema-observation-round1-challenger.md`
> at commit `6729f9bc`. The orchestrator independently reproduced C5's traceback-order claim with an
> ephemeral script (frames outermost-first; the re-raise site is at index 1, the backend frame last).

# Increment 7 round-1 defender report

Of C1–C26 I concede 13 outright (C3 C4 C5 C6 C9 C11 C13 C14 C15 C20 C22 C23 C24), and rule 13 partial (C1 C2 C7 C8 C10 C12 C16 C17 C18 C19 C21 C25 C26); none is a clean rebuttal, and two of the partials (C8, C19) fail on their stated mechanism while pointing at a larger defect the challenger missed. The load-bearing findings hold: C1's arithmetic is correct at the pin (`params(chatterbox.pick_up_tips) = {ops, use_channels} ⊆ default`, `has_var_keyword` true), C3 is confirmed by the shipped contract table (`:321`'s predicate is `Not(Cmp(Var,"==",Var))`, no `EnvRef` — `plr-sema/data/derived_contracts.json:88798-88816`), and C5 is correct on Python traceback order. But C1's pricing is wrong in the other direction: D5b is **not** an α/β/P3a/P9-class syntactic idiom, it is a semantic model of a named PLR function's local variables — a new *kind* of hand-maintained fact, and so a new-row/cap conversation, not "one or two further HM-25 units". The same is true of the only repair that makes D2 work. Two defender-identified gaps change the round materially: **Q-BIND is dead machinery** (`:409` never reaches `_eval_allof_anyof`; it goes through the α-existential path, which already binds element-wise), and **the gate needs D5b AND a repaired D2 together** — neither alone clears `pick_up_tips`'s residual. User decisions after adjudication: D1 YES (unchanged), D2 repriced and made conditional, D3 survives as a non-decision but needs a closed selection rule, D4 YES (unchanged), D5 repriced to three options, and a **new D6** that subsumes D2's repair and D5b into one question.

---

**C1 — PARTIAL (concede the mispricing, reject the class claim).**
The arithmetic is right and I verified every step. `default_args = default ∪ {"self"}` (`liquid_handler.py:351`); `args` drops `default_args` then `VAR_POSITIONAL`/`VAR_KEYWORD` (`:354`, `:360-368`); `non_default` keeps the no-default residue (`:369`) — which is set-equal to §16.3's `params` minus `default`. Chatterbox is `pick_up_tips(self, ops, use_channels, **backend_kwargs)` (`chatterbox.py:63`) and the call site passes `default={"ops","use_channels"}` (`liquid_handler.py:544`), so `non_default = ∅`, `missing = ∅` **whatever `backend_kws` is** — productions (1) and (5) of §16.1.1's box are genuinely unnecessary. For `:383`, route (a) is live and already wired: the recorded `scope_trail` carries the enclosing test verbatim, `"if len(extra) > 0 and len(vars_keyword) == 0"` (`derived_contracts.json:88906-88909`), and `evaluate_guard` returns `_SAFE` from `scope_excludes` before touching the predicate (`predicate.py:874-876`). Both sites are depth-1 SAFE and need **no** E-UNCOND lift — `fires is False → _SAFE` is unguarded by depth (`predicate.py:882-883`). **What I reject is C1's costing.** To decide either guard the evaluator must resolve `Len(Var("vars_keyword"))` and `Len(Var("missing"))`, both bound by plain assignments over comprehensions no idiom covers; a rule doing that is keyed on `(module, qualname) == (…liquid_handler, "LiquidHandler._check_args")` **and on two of that function's local names**. Nothing on HM-25 or HM-24 is keyed on a PLR function's identity — every entry is a *shape* (`_hand_maintained.py:936-971`, `:880-897`), and `why_not_derived` says so in terms ("syntactic patterns over how PLR is WRITTEN … not facts PLR records anywhere about itself"). D5b is a hand-written contract for one named function, which is §8's class, not HM-25's. Consequently `_measure_hm25`'s import-the-symbol measure covers only *deletion of the rule*; it cannot see PLR renaming `vars_keyword`, so the loud half must be a published count (`n_check_args_decided == 544`), not the import. **Endorsed remedy:** reprice §16.1.1 as C1 asks (three options), and state D5b's honest cost as **~100–130 LOC plus a new registry row against a full `BUDGET_CAP` 24** (or an HM-25 `what` widening that changes the row's own kind) **plus C15's decorator gate**, not "one or two HM-25 units". Recommendation moves from NO to *user's call* — see D6.

**C2 — PARTIAL.** The gate is not vacuous — §16.10.4's last paragraph makes "`scope_verdict == SAFE` on ≥ 1 op without D5" the document's own cheapest falsifier, which is a real one-number test in the direction that matters. But under C1 the user is being asked to accept a pre-called NO-GO on a premise that is false, and there is a second reason the challenger missed: even with D5b, `:321` remains ½ without a **working** D2, so `scope_verdict` stays UNKNOWN. The gate is winnable only under D5b *and* a repaired D2. **Endorsed remedy:** keep the `scope_verdict == SAFE` clause, and add the conjunctive floor C2 proposes as the NO-GO-side criterion so the increment can fail on its own terms either way; state the D5b+D2 conjunction explicitly in §16.10.2.

**C3 — CONCEDE.** Confirmed at the cited lines and better. `substitute` replaces **only** α-bound `Var`s (`derive/bindings.py:232-237`); a β binding and an unbound name are returned untouched (`:237`). The shipped guard record for `:321` is `Not(Cmp(Var("resource_from_deck"), "==", Var("resource")))` with `bindings: []` and `free_vars: []` (`derived_contracts.json:88797-88816`) — R-DECK's shape `Cmp(EnvRef(("self","deck","get_resource"),(t,)), "==", u)` occurs nowhere. The second half is confirmed too: `scope_trail` is `["if not resource_from_deck == resource", "for resource in resources"]` (`:88819-88822`), so `resource` is a `for` target and nothing in this increment binds one. **Endorsed remedy:** withdraw R-DECK as specified. Do **not** add the two productions C3 prices (a δ plain-assign idiom + `for`-target element-wise binding); the cheaper and honest repair is a **site rule on `(LiquidHandler._assert_resources_exist, :321)`** — the guard is `F` iff every element of the caller-bound `resources` has a name in `deck_resource_names`. `resources` binds cleanly (`self._assert_resources_exist(tip_spots)` at `liquid_handler.py:520`, `tip_spots` is `pick_up_tips`'s own parameter → a `Seq` of `ir.Ref`s), so the rule has what it needs. That makes D2 the *same class of fact* as D5b, which is why they should be one user question.

**C4 — CONCEDE.** Confirmed contradiction inside the document: `AC-16.9`'s gated floor is "≥ 2,170 — 1,563 plus `:409`'s 384 plus `:514`'s 223" (draft `:1284-1286`) while `:1099-1108` declines to claim the 384 ("223 certain and up to 384 … which no number anywhere covers"). A prediction-holding run scores 2,009 and fails its own gate. `n_clusters 53 → 51` carries the same assumption — a partially-cleared cluster is still a cluster. **Endorsed remedy:** exactly C4's — gate at **≥ 2,009**, publish 2,170 as target, split `guard_env_dependent` and `n_clusters` into 223-certain and up-to-384 variants.

**C5 — CONCEDE, and it is the cleanest blocker in the report.** `extract_tb` returns outermost-first; each frame an exception propagates through is *prepended*, so `[-1]` is the innermost — the original raise site. At `:576` the object was caught at `:554-555` from the `await` at `:553`, so its `__traceback__` already ends in the backend's own raising line; `raise error` prepends a second `pick_up_tips` entry at `:576`. `[-1]` is therefore the backend frame and can never equal a `PlrSite` in `excludes_sites`. F2 excuses nothing, `rows_excused_by_frame` is structurally 0, and the defect is masked by the predicted zero-`SAFE` population. **Endorsed remedy:** C5's — capture `error_frames: list[...]`, excuse iff **any** frame matches, with the normative tie-break naming the outermost PLR-layer match; AC-16.8 gains the backend-raise-then-`:576`-re-raise fixture.

**C6 — CONCEDE.** Increment 6 G8(1) is flat and normative: "`AllOf`/`AnyOf` over a ⊤ seq is ½, **never vacuously `T`**" (`260904_plr-sema-predicate-increment.md:630-634`), and the shipped code says the same in a comment and in `_eval_allof_anyof` (`predicate.py:576-578`, `:600`). Q-MONO's `AllOf(⊤, T) = T` overturns the sentence; §16.5.5's "preserved in both directions" is a re-reading. The rule itself is sound. **Endorsed remedy:** C6's — an explicit amendment box against §15.2 G8(1) and §15.4 A-C3, and the increment-6 spec file added to T43's file list (T48 already sets the precedent for editing an earlier increment).

**C7 — PARTIAL (concede the invariant, note it survives Q-BIND's removal).** C7 is right that the claim is false as written: for a concrete seq with `n ≥ 1` the shipped evaluator already returns a definite value (`predicate.py:606`), so Q-BIND replaces a definite value, not a ½. I checked the invariant C7 asks for against every shipped production and it holds **vacuously today**: `_eval_is_instance` returns `None` on a non-`Ref` (`:370-372`), `_eval_is` requires a `Lit` (`:568-571`), `_eval_cmp` returns `None` on membership and on any unresolved `Len` (`:542-543`, `:555-556`, `:558`), `_maybe_setof_uniqueness` requires a `Seq` of hashable `Lit`s (`:504-516`), `_eval_alpha_existential` returns `None` unless the iterand is a concrete `Seq` (`:441-443`). R-CONST is the first deliberate exception and Q-MONO the second — and the invariant as C7 phrases it covers both, since `AllOf(⊤ seq, T)` is independent of the seq's value. **Endorsed remedy:** C7's, and note the invariant is required for R-CONST whether or not Q-BIND ships (see D1 below).

**C8 — PARTIAL (premise wrong, conclusion right for a stronger reason).** G3 does **not** construct an `AnyOf` node at rewrite time. `:409` is evaluated by `_maybe_alpha_emptiness` → `_eval_alpha_existential`, which loops over the iterand's real items and overrides the bound name per element (`predicate.py:459-480`, `:433-456`, the override at `:450`); the module docstring says so explicitly and distinguishes this path from `_eval_allof_anyof` (`:402-410`). So there is no `AnyOf.target` to inherit at `:409`, and no target-inheritance sentence is needed. The conclusion — an implementer wiring `target` gets today's behaviour at `:409` — is right, because `:409` never reaches Q-BIND's code path at all. **Endorsed remedy:** not C8's; see defender gap D1.

**C9 — CONCEDE, and it moves more than C9 claims.** `_resolve_var` returns `("operand")` for any α/β binding regardless of value and `("env")` only when nothing was found (`predicate.py:245-255`, docstring `:219-227`), and `guard_reason` branches on exactly that (`:704-714`). `:383`'s free `strictness` **is** a parameter of `_check_args`, so under `caller_args` it acquires a home; if the spec chooses `"operand"`, `:383`'s 544 findings move from `guard_env_dependent` to `guard_operand_unknown`, which is a ~544-finding swing across two of §16.10.3's aggregate cells and one of the gate's published zero-conditions. **Endorsed remedy:** C9's normative clause, plus the corrected `guard_operand_unknown` prediction (144 → its new value) in §16.10.3.

**C10 — PARTIAL.** The mechanism is confirmed: `compute_local_bindings_for_guard` gates on `first_stmt.lineno < guard_lineno` and on `_ancestors_are_prefix(first_chain[:-1], guard_chain)` (`derive/bindings.py:723-725`), both computed in one function's line space. Applying it with the delegate's guard lineno against `K`'s body is meaningless, and for a delegate defined *below* its caller it silently admits a rebinding written after the call — a real unsoundness. But C10's "the prediction fails" is wrong: `:409`'s `use_channels` resolves through the P3a channel hook (`predicate.py:236-239`), which is context-level and lineno-independent, so the 223-op prediction survives even under the broken reading. **Endorsed remedy:** C10's normative clause (caller-side position/scope tests use the **call statement's** lineno in `K`) and the delegate-below-caller fixture; drop "the prediction fails" from the finding.

**C11 — CONCEDE.** §16.2.3's box says "every existing test, every shipped fixture, the graph lane, and any caller that does not thread an observation are **behaviourally identical to today by construction**" (draft `:420-425`), which follows only from "every §16.5 rule declines". After T42 the argument map resolves depth-1 names with `env` empty and D1's lift can emit `WILL_FAIL` with `env` empty. AC-16.1 is gated on T40 where the claim holds. **Endorsed remedy:** C11's scoping, verbatim.

**C12 — PARTIAL, and a worse problem sits behind it.** The int-sort half is over-read: the spec says values are "a list as its **sorted** members" and the members of `head_channels` are `int`s, so `sorted()` is numeric — one clarifying clause, not a defect. The `,`/`=` injectivity half is real for `deck_resource_names`, which carries user-chosen names. **But §16.5.6 threads the deck observation to the evaluator as a per-slot boolean built by `resources_from_example`, not as an `obs:` member**, and §16.2.3's canonical-encoding example lists only three members — so under D2 a fact that changes the verdict may not enter `cache_key` at all. That is a cache-correctness event, strictly worse than non-injectivity. **Endorsed remedy:** C12's escaping/JSON encoding, **plus** a normative sentence resolving whether `deck_resource_names` enters `env`; if it does not, state what does (a digest of the per-slot map).

**C13 — CONCEDE.** `verify()` has three return sites: the success return at `:184-200`, and the deck-build early return at `:150-162` where `setup is None` and every observation field is unobtainable — which returns `volume_tracking_observed` at its default (`training/verify/verifier.py:161`), exactly the precedent C13 names. AC-16.1's "returns `plr_observation` with exactly the four fields" is unsatisfiable there. **Endorsed remedy:** C13's — `plr_observation` is `None` on that path and whenever any field's read raises, which also agrees with §16.5.1's partial-record refusal.

**C14 — CONCEDE.** `snapshot()["topology"]` is `self.deck.serialize()` (`training/verify/deck.py:153`), a nested serialization, while `Resource.get_resource` matches the deck's own name plus every descendant recursively. The extraction is the load-bearing half of D2's observation and is unspecified. **Endorsed remedy:** C14's — specify the recursive extraction, or read child names directly inside the window (`SetupHandle` already walks the tree in `iter_tracked`/`well_index`, `deck.py:128-144`, so the pattern exists).

**C15 — CONCEDE, and it is D5b's soundness gate too.** `constant_return` is an AST fact and R-CONST claims a runtime value; nothing in §16.3 excludes a decorated definition. `functools.wraps` is safe (`inspect.signature` follows `__wrapped__`), but a non-`wraps` decorator is not, and the same gap sits under §16.3's `params`/`has_var_keyword` — which is precisely what D5b would read. Note the abstract base's `can_pick_up_tip` is `@abstractmethod`-decorated (`backends/backend.py:183-187`), so the rule bites immediately. `staticmethod`/`classmethod` are covered by the same test. **Endorsed remedy:** C15's — a row is **absent** when `decorator_list` is non-empty, the definition is a `property`, or the qualname is defined at more than one lineno; negative fixture in AC-16.2.

**C16 — PARTIAL.** The first half stands: "or as a key of some class's own parameter set reachable from one" (draft `:452-456`) defines neither "parameter set" nor the reachability relation, and no AC tests it, which makes AC-16.2's "complete measured selection" unfalsifiable. The second half I reject: §16.2's four harness-side reads ride increment 5's `volume_tracking_observed` precedent, which reads a named PLR global from inside the window and books no registry row — the registry's scope is the analyzer's own front end (`_hand_maintained.py:976-983`, "patterns over how PLR/its own analyzer is written"), not the harness. **Endorsed remedy:** define the selection as one closed rule with its own published count; add C16's one sentence stating that harness-side observation paths are outside registry scope **and citing the increment-5 precedent** — the spec currently asserts the cost is zero without naming why.

**C17 — PARTIAL.** The window claim is right in direction: `build_setup` is at `verifier.py:117`, `machine.setup()` at `:132`, and the inner `try` wraps only `_execute` (`:133-136`) — a read placed "after `build_setup`, before `_execute`" that raises is caught by the **outer** handler at `:143` and converted into a harness-level `error`, not an operation error. But the risk is small (`num_channels` is a plain property, `chatterbox.py:59-61`) and it is not clear it reaches `rows_setup_error`, which the challenger asserts without citing its definition. **Endorsed remedy:** take C17's remedy anyway — one capture point after `await setup.machine.setup()`, inside a fail-closed guard setting `plr_observation = None` — because it also discharges C13 and simplifies §16.2.1's two-window normative box to one.

**C18 — PARTIAL.** The fence *is* evidence, but of a narrower proposition than §16.15's D2 row claims. On each of the 288 operations, if A-DECK-OBJECT had been false the guard would have fired, PLR would have raised, and a `scope_verdict == SAFE` row would have been counted unsound (`plr-sema/eval/oracle_common.py:767-786`) — so the fence genuinely checks *the corpus*, in exactly the way A-ENABLED's own partial discharge works. What it does not check is the assumption in general, and §16.11 proves in the same document that no available mutator can construct a violation. Both statements are true; the D2 row states only the flattering one. **Endorsed remedy:** C18's rewording ("its exposure is bounded by argument on the corpus, not by adversarial measurement") plus the hand-built duplicate-name fixture. This changes D2's *basis*, not its sign.

**C19 — PARTIAL; the blocking half is rebutted on the evidence.** `scope_trail` **does** record loop headers: `:321`'s trail is `["if not resource_from_deck == resource", "for resource in resources"]` (`derived_contracts.json:88819-88822`). `_scope_entry_value` gives a `for`/`while` header ½ and never `F` — `ast.parse` on the header text is a `SyntaxError` → `Opaque` → ½ (`predicate.py:724-738`) — and `_entry_satisfies_uncond` therefore returns `False` for it (`:760-770`). So an in-loop guard has a non-empty trail with an unsatisfiable entry and can never pass `guard_is_unconditional`'s `all(...)` (`:788`). This also answers C19's attack on R1-C4: clause (5) is the **empty-trail** case only (`:787`), and a guard inside a loop cannot have an empty trail, so "the loop body never runs" is not a missing disjunct in R1-C2's enumeration. The lift keeps the same check on both `D`'s own trail and, by precondition 2, on `caller_scope_trail`. What survives is that `compute_reachability_clear` itself does not test loops (`derive/bindings.py:812-829`) and returns `True` for the in-loop `:321` (`derived_contracts.json:88818`) — sound only because the trail check carries it. **Endorsed remedy:** the last sentence of C19's remedy only — one normative sentence in §16.4 and §16.15 Q6 stating that `scope_trail` records `for`/`while` headers, that such an entry is unsatisfiable under ways (1)–(3), and that this is what protects in-loop guards. **No fourth precondition and no sixth AC-16.4 fixture.**

**C20 — CONCEDE.** Both identities are unstated and both are real: `PlrSite`'s lineno is the raise statement's own line (`…liquid_handler.py:321:LiquidHandler._assert_resources_exist`, `unknown_ledger_260909_after.json:179`), and the frame's file is a runtime `__file__` that equals the submodule-relative path only when PLR resolves to `external/pylabrobot/`. **Endorsed remedy:** C20's — one normalisation helper, both identities normative, AC-16.8 asserting a match against a real `PlrSite` string.

**C21 — PARTIAL.** Correct that Q-MONO has no counter of its own. But under defender gap D1 the counter set changes: with Q-BIND withdrawn, the ask is `n_quantifier_decided_by_qmono` (predicted 223 at `:514`) and `n_membership_decided` (already specified, predicted 384) — not two new quantifier counters. **Endorsed remedy:** add `n_quantifier_decided_by_qmono` per site and whole-table to §16.10.1 block (1), with AC-16.11 coverage; drop the Q-BIND counter.

**C22 — CONCEDE, verbatim.** Increment 6 §15.6 states the condition in one sentence: *"to move either `:375` or `:383` … the increment must AST-derive the backend class's method signatures"* (`260904_plr-sema-predicate-increment.md:1478-1480`). §16.3 ships exactly that, and §16.1.1 then declares the sites undecidable on a different ground without noting it. Increment 6 even flags the `:377-378` early return as un-recorded (`:1495-1498`) — which §16.1.1 route (b) repeats, while the recorded trail makes route (a) sufficient and the early return unnecessary. **Endorsed remedy:** C22's reconciling paragraph, and the reconciliation is that §15.6 was right.

**C23 — CONCEDE.** "≥ 1 achieved with 0 unsound" has no denominator, against a p1 precedent that publishes 288/288, 16/16, 0/288 (`predicate_mutants_260909_inc6.json:2-58`), and against the document's own argument that a class which can only report 0 is a publication, not a gate. **Endorsed remedy:** C23's — publish `achieved/attempted` and set the floor with the attempted count named.

**C24 — CONCEDE.** R-HEAD and R-CONST are as observation-dependent as R-DECK, so the lane asymmetry is general. And tier 2b *can* observe: `region_oracle` builds every fixture on one backend, `build_setup("LiquidHandlerChatterboxBackend", layout)` (`plr-sema/eval/region_oracle.py:420`) — whose `can_pick_up_tip` is the constant-return case and whose head is the 8-channel one — so R-HEAD and R-CONST will move tier 2b as soon as T40 threads the observation there (which T40's file list says it does). §16.11's "the rule that could move tier 2b is Q-MONO" is therefore incomplete. **Endorsed remedy:** C24's, verbatim.

**C25 — PARTIAL.** C25 is right that R-ATTR's stated purpose (head/num_channels agreement) is a harness assertion, and right that a rule with predicted-zero reach inside a table costing a ceiling unit is the shape §9.4 targets. But D4's ask is **one** unit for the pattern *"an `EnvRef` path admitted against the observation record"* regardless of instance count (§16.9's own "one pattern with four instances" argument), so dropping R-ATTR does not reduce the ask, and keeping it means the next observation field needs no new rule. This is a presentation choice, not a defect. **Endorsed remedy:** add C25's `len(head_channels) == num_channels` harness assertion to AC-16.1 either way; make keeping-or-dropping R-ATTR an explicit sub-note under D4 rather than a silent inclusion.

**C26 — PARTIAL.** T42's stated dependency on T41 is indeed unexplained — `compute_caller_args(K, D)` reads `K`'s AST and `D`'s `ast.arguments` from the function index and touches no backend surface; the dependency should be dropped or justified. The two by-assertion refusals are a fair style note but not a defect (both point at standing dispositions with their own documents). The bundling point is real: a D1 decline changes T42's shape mid-row rather than removing a row. **Endorsed remedy:** drop or justify the T41 dependency; state in T42 exactly which files and LOC are withdrawn on a D1 decline; one sentence apiece for the two refusals.

---

## Defender-identified gaps

**D-G1 — Q-BIND is dead machinery, and it is the most expensive clause in §16.5.** §16.1.2's closing paragraph and §16.10.3's `:409` row both claim `:409` needs Q-BIND because "the shipped evaluator evaluates the body once with every comprehension-bound name at ⊤ (`predicate.py:597-606`)". That is the wrong code path. `:409`'s guard is `Not(Cmp(Len(Var("invalid_channels")), "==", Lit(0)))` with an α binding on `invalid_channels` (`derived_contracts.json:89105-89153`); `_eval_cmp` dispatches it to `_maybe_alpha_emptiness` → `_eval_alpha_existential`, which loops over the iterand's real items and overrides the bound name per element (`predicate.py:536-538`, `:459-480`, `:433-456`, override at `:450`) — **element-wise already**, and the module docstring at `:402-410` says so and distinguishes it from `_eval_allof_anyof` by name. `:409` needs exactly three things: `channels` resolving to a `Seq` (§16.4), R-HEAD in **term** position (§16.5.1), and the membership deciding case at `_eval_cmp:542-543` (§16.5.4). It needs **no** `target` field, no `parse` half, and no reversal of G8(1)'s "recorded nowhere" sentence. Withdrawing Q-BIND removes ~50 LOC from T43, removes an additive field from three wire nodes, defuses C8 entirely and shrinks C6 and C7 to Q-MONO alone. If the author believes another site needs Q-BIND, §16.5.5 must name it; at this pin I find none.

**D-G2 — the gate needs D5b AND a repaired D2, and the document never states the conjunction.** `pick_up_tips`'s residual is six sites, all blocking all 223 ops (§16.0.1, confirmed against `unknown_ledger_260909_after.json:41-45, :90-94, :136-148, :177-188, :217-223, :257-261`). With `:576` excluded and `:409`/`:514` flipped, `scope_verdict` still joins `:375`, `:383` **and** `:321`. So C1's "the gate becomes winnable" is true only under D5b **plus** a D2 that actually fires — and C3 shows the specified D2 does not. Both repairs are the same kind of fact (a site rule keyed on a named PLR function). **This is one user question, not two**, and it should be put as one.

**D-G3 — the deck observation may never enter the cache key.** §16.2.3 enumerates the benchmark's `env` members as exactly three (`obs:backend_class`, `obs:num_channels`, `obs:head_channels`) while §16.5.6 threads `deck_resource_names` to the evaluator as a per-slot boolean from `resources_from_example`. If that path bypasses `env`, a D2-enabled verdict is not a function of the fifth `cache_key` component and §16.6's claim (4) ("the verdict is a function of `env`") is false under D2. §16.2 and §16.5.6 must be made to agree.

**D-G4 — `unknown_ledger` is not asked to carry `scope_verdict`.** §16.10.1 block (7) asks the after-ledger only for reason counts, clusters and the per-op reason-set histogram; block (5) puts `scope_verdict` in the replay report alone. Since §16.0.1's normative box makes `scope_verdict` per operation "the only number in this document allowed to decide GO", the ledger — the instrument the increment is written against — cannot audit its own gate. T46 should thread `scope_verdict` into `unknown_ledger.py` (already in its file list) or say explicitly why the replay report alone suffices.

**D-G5 — `t30_measure`'s prediction role is unmaintained.** §16.10.3's `n_membership_cmp = 48` is read off `t30_measured_260908.json`, and `classify_guard_structural` is a structural predictor of what `E-CALL` will decide (`plr-sema/eval/t30_measure.py:875`, `:1014`, `:1472`). No task row re-runs it, and the increment changes what "decidable" means for exactly the classes it partitions on (`AllOf/AnyOf`, `Filtered-emptiness`, `:343-356`). Either T46 re-runs it and republishes, or §16.10 states that its prediction role is retired for these classes.

**D-G6 — `:375`/`:383` can never emit `WILL_FAIL`, and saying so strengthens D5b.** Both guards carry `reachability_clear: false` (`derived_contracts.json:88870`, `:88905`), because of the `return set()` at `liquid_handler.py:349`. Combined with `evaluate_guard`'s unguarded `fires is False → _SAFE` (`predicate.py:882-883`), the D5b discharge is **one-directional by construction**: it can only ever add `SAFE`, never a false positive, and it needs no interaction with D1. That is the strongest available argument for D5b and the document does not have it.

---

## The user decisions after this round

| id | recommendation AFTER adjudication | one-line reason |
|---|---|---|
| **D1** — lift `E-UNCOND(4)` at depth 1 | **YES, unchanged** | C19's missing-precondition attack is rebutted on the evidence — `scope_trail` records `for` headers and they are unsatisfiable under ways (1)–(3) — and C10/C11 are scoping fixes, not soundness defects |
| **D2** — A-DECK-OBJECT + deck observation | **CONDITIONAL on D6, and repriced** | R-DECK as written cannot fire (C3, confirmed against the contract table); the only repair is a site rule of D5b's class, so D2 is no longer ~110 LOC and no longer a standalone ask |
| **D3** — §16.3 hand-types nothing | **Stands as a non-decision** | No literal PLR fact is hand-typed and the harness-side reads ride increment 5's `volume_tracking_observed` precedent — but the selection rule's second clause must be closed (C16) before AC-16.2 is falsifiable |
| **D4** — HM-25 `declared` 9 → 10 | **YES, unchanged** | The unit buys the path-shape table whether the table has three shapes or four; C25 is a presentation improvement, and withdrawing Q-BIND (D-G1) does not touch this row |
| **D5** — model `_check_args` | **Repriced to D5a / D5b / D5c; recommendation moves from NO to "D5b if D6 is taken"** | C1's arithmetic holds at the pin and the SAFE direction needs neither production (1) nor (5); D-G6 shows it is one-directional by construction |
| **D6 (NEW)** — accept **site-keyed semantic models of named PLR function bodies** as a class of hand-maintained fact | **User's call; the sprint recommends YES with a new registry row** | It is the single fact both the repaired D2 and D5b need; it is *not* HM-25's kind (nothing there is keyed on a PLR qualname, `_hand_maintained.py:936-983`), so it is a **new row against a full `BUDGET_CAP` 24 — a cap conversation** — and taking it is the only way the increment's own gate becomes winnable |

---

## Ordered remediation list

1. **§16.1.1 + §16.15 D5 + §16.10.2** — reprice the `_check_args` discharge as D5a/D5b/D5c, with D5b's cost stated as a new registry row (not HM-25 units), C15's decorator gate as its precondition, and D-G6's one-directionality as its argument. *(C1, C22)*
2. **§16.15 — add D6**, stating the site-rule class once, and note that D2's repair and D5b are the same ask. *(D-G2, C3)*
3. **§16.5.6 + §16.1.3 + §16.10.3 + AC-16.13 + T48** — withdraw R-DECK as an `EnvRef` shape; respecify `:321` as a site rule over the caller-bound `resources` and the per-slot name map; drop the `≥ 2,458` floor and the `n_clusters → 50` cell pending it. *(C3)*
4. **§16.5.5 + §16.1.2 + §16.10.3 + AC-16.6 + T43** — withdraw **Q-BIND**; correct §16.1.2's claim about which evaluation path `:409` takes; keep Q-MONO. *(D-G1, C8)*
5. **§16.5.5 + §16.5.3** — state Q-MONO as an explicit amendment of increment 6 §15.2 G8(1) / §15.4 A-C3, add the increment-6 file to T43, and state C7's evaluator invariant normatively with R-CONST named as its one deliberate instance. *(C6, C7)*
6. **§16.7 F1/F2 + AC-16.8 + T45** — capture `error_frames` as a list; excuse on any frame with the outermost-PLR-match tie-break; add the re-raise fixture; normalise path and lineno through one helper. *(C5, C20)*
7. **AC-16.9 + §16.10.3** — gated floor to **≥ 2,009**, 2,170 as target, aggregate cells split 223-certain / up-to-384. *(C4)*
8. **§16.4 M2 + §16.10.3** — the `origin` clause and its corrected reason counts; caller-side position tests keyed on the **call statement's** lineno. *(C9, C10)*
9. **§16.3 + AC-16.2 + T41** — the decorator/property/multi-lineno absence rule; the closed selection rule with its published count. *(C15, C16)*
10. **§16.2.1 / §16.2.3 + AC-16.1 + T40** — one capture point after `machine.setup()`, fail-closed to `None`; the deck-build early return; the recursive `deck_resource_names` extraction; the `,`/`=` encoding and whether the deck observation enters `env`; scope the byte-identical claim to the T40 state. *(C13, C14, C17, C12, C11, D-G3)*
11. **§16.10.1 + §16.10.4 + AC-16.11** — `n_quantifier_decided_by_qmono`; the gate restated as a conjunction the increment can fail; `scope_verdict` threaded into `unknown_ledger`. *(C21, C2, D-G4)*
12. **§16.1.3 + §16.11 + §16.15 D2** — the honest A-DECK-OBJECT exposure statement plus the hand-built duplicate-name fixture; p2a's `achieved/attempted`; the lane-asymmetry disclosure covering all rules and the corrected tier-2b attribution. *(C18, C23, C24)*
13. **§16.4 + §16.15 Q6** — the one sentence on `scope_trail` recording loop headers. *(C19)*
14. **§16.9 D4 + §16.13 T42 + §16.14 + §16.10** — R-ATTR sub-note; T41 dependency dropped or justified with the D1-decline withdrawal list; one sentence apiece for the two by-assertion refusals; `t30_measure`'s prediction role. *(C25, C26, D-G5)*

## Verdict

`needs_revision` — every blocking objection except C1's pricing survives adjudication, and the two defender gaps (Q-BIND is dead, and the gate needs D5b **and** a repaired D2) change the shape of the ask rather than the spec's soundness; all of it is fixable in text plus one new decision hook, with no information missing from context.
