---
title: 'Overnight decisions 260911 -- autonomous loop re-fired after sprint 133 NO-GO; sprint 135 selected and run as the first surface-dispatch pilot'
description: 'L5 decision log for the 260911 autonomous loop run (task 260911_sema-volume-wiring). Every call made without the user, with enough context to backtrack: the increment 9 / :2070 question left with the user; six shipped-but-open Coxswain items closed as stale; #5043 picked over three extended REPL-refocus P1s; bugfix_simple template, no brainstorm, adversarial TIER1; fixer routed to Cursor over ACP via surface_dispatch with containment none accepted and recorded; a transient disk-full that killed the first dispatch; two praxia-side dispatch bugs found and worked around; the worktree guard forcing commit-after-copy-out; T58 measuring the item''s own inflation hypothesis FALSE.'
status: completed
task_id: 260911_sema-volume-wiring
date: '260911'
---
# Overnight decisions, 2026-09-11

Loop prompt: `/loop /praxia:orchestration /praxia:autonomous-praxia-dev-loop and use surface dispatch`.
No human input arrived during this run; every `<task-notification>` was a system event. Each entry
below is a decision I took alone, in the order taken.

| # | decision | why | how to undo |
|---|---|---|---|
| 1 | **Did not start increment 9 / did not answer the `:2070` question.** Sprint 133's NO-GO (§17.8.2 condition (1)) was escalated to the user by the spec's own instruction. Re-firing the loop is read as "keep working", not as a decision on that question. | The spec says "bring the decision to the user"; TRIAGE can pick independent work without presuming it. | Nothing to undo; the question is still open in PR #158's body and plan §9. |
| 2 | **No FSM instance scaffolded.** `loop_state status` returned zero instances and no `loop_state.toml` exists; sprints 121–133 all ran with the `sprint`/`backlog` tables as state. | Scaffolding a DB FSM mid-thread (with human-only `advance` semantics) would add a state carrier nobody else in this thread uses. Recorded as a gap, not fixed. | `praxia loop migrate` from a seed TOML if the FSM is wanted. |
| 3 | **Closed backlog #4389–#4394 as `completed`** (Coxswain W1.0–W5) with commit citations `71717536`, `e22e9968`, `e84f8010`, `c42adc1f`, `9d7b24d9`/`b4bffc5e`, `4827f3f0`/`2e1a2373`/`2dc8c3a7`. | All six are on the branch; `repl.yml` already carries the W1.0 `coxswain` job. This is the idea-351 staleness gap in the wild. | `backlog update` each back to `open`. W6 (#4395) deliberately left open (conditional on W0). |
| 4 | **Selected #5043 as sprint 135** over the three executable P1s (#4293/#4294/#4296, REPL refocus). | All three are `extended`, need headless Chromium + Pyodide under a disabled sandbox, and #4293's S-E needs a human with a device. #5043 is `standard`, real (verified line by line), fail-closed-to-less-closed, and bears on counts every later increment cites. | Cancel sprint 135; reopen #5043. |
| 5 | **Rubric route: no BRAINSTORM, adversarial TIER1, template `bugfix_simple`.** | parts/01_triage.md: `category=bug` → `brainstorm_needed=false`; `category in {bug, debt}` → TIER1; `bug` → `bugfix_simple`. | Author a spec and run a challenger round if wanted. |
| 6 | **`pm_triage` ran as mandated and produced NO record**: fork/join hit the 900 s walltime with 2 of 4 branches done; the `backlog_triage` branch exhausted retries failing to emit a parseable `{reasoning, action}` for `write_backlog_triage_result` (one attempt stuffed a whole JSON payload into `debt_id`). Treated as the flow failing honestly on Qwen3.8-27B; the orchestrator's own backlog scan (logged as `260911_loop_backlog_scan_and_recon`) stood as the triage. No pm agent re-dispatched mid-sprint. | The skill's fallback rule; a redundant Sonnet pm pass would not have changed a rubric-determined route. | Re-run `pm_triage` on a stronger backend before the next sprint. |
| 7 | **Praxia sprint id is 135**, not the 134 the triage text guessed. Plan/docs say 135. | DB-assigned. Same lesson as sprint 130 vs "128". | -- |
| 8 | **Fixer routed to Cursor over ACP (`transport: acp`, explicit), `standard` tier = `cursor-grok-4.6-medium`.** | The user asked for surface dispatch. `auto` resolves straight to headless (debt #1762). ACP is the transport live-tested against exactly this `cursor-agent` build (`2026.09.10-fd3934a`). Headless would add `--sandbox enabled` but puts `uv`'s cache/network inside a restricted terminal. **Accepted and recorded: `containment` read `"none"`; the permission engine is known not to be consulted by this Cursor build. Admission's git checks plus the orchestrator's own diff + test run were the real gate.** | Re-dispatch with `transport: headless` (fresh `dispatch_id`; worktree clean at base). |
| 9 | **Dispatch worktree `autoloop-260911_sprint135`, branch `plr-sema-volume-observation-wiring`, based at `12003b06`** (PR #158's tip). A fresh worktree has no `external/pylabrobot` submodule, so `uv sync` fails there; **the fixer's step 0 was the submodule init + `uv sync --all-packages`**. The seeded `.praxia/workspace.id` was removed again because an unclaimed untracked file fails admission's unscoped check. | Base must be the stack tip because `plr-sema/` is not on `main`. | `git worktree remove --force`; delete the branch. |
| 10 | **First dispatch (`cfc81e4e`, attempt 0) died of a transient ENOSPC on the root volume** at 17:50Z: admission's `git status` failed writing the submodule `index.lock` ("Out of diskspace") and the terminal record's append failed ("No space left on device"). `df` a minute later showed 92–96 GB free, host C: 695 GB free, disk-guard in backoff. Cause of the spike not identified. | Environment, not the agent. | -- |
| 11 | **Two praxia-side dispatch bugs found, worked around, NOT fixed here (wrong repo).** (a) The retry contract (`attempt` 1, same id) refuses itself through the MCP surface: `surface_dispatch run` writes `envelope.json` into `.praxia/dispatch/<id>/` before spawning, and the child's retry precondition then counts that file (and `.praxia/dispatch/.lock`) as unclaimed untracked paths — so a retry can never pass. (b) The same `envelope.json` shows up in the Completed record's `git_status_unscoped`, i.e. the materialization allowlist omits it. Workaround: fresh `dispatch_id` at `attempt` 0 after deleting the stale dispatch directory. | Both belong in the praxia repo's `praxia_dispatch::materialized_paths` / the MCP `run` handler. | File as debt in praxia; see the sprint plan §4. |
| 12 | **The Cursor agent left a `.serena/` cache at the worktree root** (not gitignored) on the first attempt; removed by hand, and the brief was amended to make the agent delete tooling caches before finishing. It did (`rm -rf .serena` appears in its tool log). | Unclaimed untracked paths fail admission. | -- |
| 13 | **Completed dispatch `e8e410a9` carried `claim_verified: null` and `tests: []`** (the ACP surface returns no structured self-report), so the record is an unchecked model claim by the tool's own definition. **Verified by hand instead**: `diff -u` of the three files against the identical base in this worktree, and an independent per-file pytest run inside the dispatch worktree (75 passed) and again here after copying (75 passed). | Admission cannot verify what the surface does not report; the orchestrator must. | -- |
| 14 | **Commit strategy forced by the worktree guard.** The session's isolation hook refuses any git command targeting a sibling worktree, so the fixer's uncommitted edits were copied out, the dispatch worktree removed, the sprint branch checked out in *this* worktree, the files copied in, and committed here (`80dd7103`). | Only path that keeps every safety hook intact. | -- |
| 15 | **T58 re-measure run in-session** (`unknown_ledger.py`, identical recipe to the inc8 header; ~90 s). **Result: nothing moved. #5043's inflation hypothesis is measured FALSE**; the wire is live (`probe_volume_observation.py`: `True` on 30/30 executed rows) but the env member is consumed only on a *firing* volume guard, and no benchmark row has one. Published ledgers since increment 5 stand. Recorded as the sprint's outcome, not as a failure. | A measurement is a measurement. | -- |
| 16 | **Two probe scripts promoted to tracked scripts** (`plr-sema/scripts/compare_unknown_ledgers.py`, `probe_volume_observation.py`) because their output is cited in the plan (ephemeral-scripts rule). Ruff-formatted; lint not enforced on `plr-sema/scripts` by the root config and sibling scripts carry their own violations, so `print` was left. | Reproducibility of cited numbers. | -- |
